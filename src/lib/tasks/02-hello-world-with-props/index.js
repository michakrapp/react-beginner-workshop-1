import React from 'react';

// every react components/(function) first argument is an object with properties (props).
// this one receives the prop named "message"
// eg. {message: "foo"}

// export a function which returns a html tag with the message prop value,
// provided from its parent.

// tip: to display (or evaluate) plain variables, such as strings and numbers, simply wrap the variable
// with curly braces

export default // your function here